---
interface Props {
  type?: "note" | "tip" | "caution" | "danger";
  title?: string;
}

const { type = "note", title } = Astro.props;

const typeConfig = {
  note: {
    icon: "ℹ️",
    bgColor: "bg-blue-500/10",
    borderColor: "border-blue-500/50",
    titleColor: "text-blue-400",
  },
  tip: {
    icon: "💡",
    bgColor: "bg-emerald-500/10",
    borderColor: "border-emerald-500/50",
    titleColor: "text-emerald-400",
  },
  caution: {
    icon: "⚠️",
    bgColor: "bg-yellow-500/10",
    borderColor: "border-yellow-500/50",
    titleColor: "text-yellow-400",
  },
  danger: {
    icon: "🚨",
    bgColor: "bg-red-500/10",
    borderColor: "border-red-500/50",
    titleColor: "text-red-400",
  },
};

const config = typeConfig[type];
const displayTitle = title || type.charAt(0).toUpperCase() + type.slice(1);
---

<div class={`aside my-6 p-4 rounded-lg border-l-4 ${config.bgColor} ${config.borderColor} not-prose`}>
  <div class={`flex items-center gap-2 font-semibold mb-2 ${config.titleColor}`}>
    <span>{config.icon}</span>
    <span>{displayTitle}</span>
  </div>
  <div class="text-gray-300 prose prose-sm prose-invert max-w-none">
    <slot />
  </div>
</div>
